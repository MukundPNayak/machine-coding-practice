Key things

1) useState initialisation syntax : useState(()=>{
    return initialState
})

2) Defining css variables is done using --varname

3) :root {

} -> This indicates define at root level

4) data-theme attribute is set on body, If an element has data-theme="dark", override these variables on that element. So when data-theme is dark bg-color will be black

5) Building toggle
  <label> for outer container and to make checkbox click work everywhere. otherwise it works only on small portion
    <input checkbox> for accessibility and state handling. Display will be done
    <div> for circle visual
  </label>


https://www.greatfrontend.com/questions/user-interface/like-button?language=js&tab=coding
